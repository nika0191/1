---
name: restoration-timelapse-shorts
description: Производство и автоматизация YouTube Shorts в формате AI-таймлапса восстановления (заброшенные здания, крыши, поезда, вагоны, башни, машины, усадьбы). Идея → «якорный» кадр разрухи → та же статичная камера, объект меняется в кадре по стадиям (Google Flow / Veo, Grok Imagine, FLUX) → сборка 9:16 через ffmpeg → заголовок, описание, хэштеги, раскрытие AI. Используй, когда просят «сделай шортс про восстановление», «таймлапс реставрации», «придумай идеи для канала восстановления», «промпты для Flow/Grok под ремонт», «собери ролик из клипов», «контент-план для канала реставрации» — даже если слово «таймлапс» не прозвучало.
---

# Restoration Timelapse Shorts

Скилл ведёт один ролик (или пачку) от идеи до готового MP4 1080×1920 и пакета метаданных.
Главный принцип ниши: **смотрят не ремонт, а историю объекта**. Безымянная «Abandoned Villa Restoration»
в 2026 году собирает сотни просмотров; реальное место, неожиданное превращение или человек,
для которого строят, — миллионы. Подробности и цифры: `references/niche-analysis.md`.

## Файлы скилла

| Файл | Когда читать |
|---|---|
| `references/niche-analysis.md` | Выбор темы, идеи, «чего нет у конкурентов», заголовки |
| `references/prompts.md` | Генерация кадров и видео: шаблоны промптов для Grok, Flow/Veo, FLUX |
| `references/setup.md` | Подключение сервисов, что куда входит, что докупить |
| `scripts/new_episode.py` | Создаёт папку эпизода с планом и готовыми промптами |
| `scripts/assemble.sh` | Склейка клипов в Shorts 9:16, скорость, музыка, хук-текст, проверка петли |

## Пайплайн

### 1. Идея (5 мин)
Прочитай `references/niche-analysis.md`, раздел «Формула темы». Тема обязана пройти фильтр:
- **Конкретный объект** (не «старый дом», а «водонапорная башня 1912 года в Выборге», «вагон-ресторан СВ 1970-х»),
- **Крючок-превращение** (восстановить как было / переделать во что-то неожиданное / для кого-то),
- **Одна деталь-интрига в первом кадре** (дерево растёт сквозь крышу, паровоз в лесу по колёса в земле).

Если пользователь просит идеи — выдай 10 штук по формуле и отметь 3 лучших. Для проверки спроса
можно вызвать `mcp__vidIQ__vidiq_keyword_research` и `mcp__nexlev__youtube_search` (type=shorts, sort_by=views).

### 2. Эпизод
```bash
python3 .claude/skills/restoration-timelapse-shorts/scripts/new_episode.py \
  --slug water-tower-vyborg --object "1912 brick water tower on a hill" \
  --place "Vyborg, Russia" --twist "restored as it was" --stages 6
```
Создаётся `episodes/<slug>/` с `plan.md` (стадии, промпты, чек-лист, метаданные) и папками
`frames/`, `clips/`, `audio/`, `out/`.

### 3. Кадры стадий (ГЛАВНОЕ — статичная камера, один объект)
Это формат, который реально набирает миллионы у Bau Rausch, Structural.Aesthetics, Map Snap:
**одна фиксированная камера, один объект, который меняется в кадре стадия за стадией.** Метод
«**одна камера — много состояний**»:
1. Сгенерируй **якорный кадр** — разрушенное состояние объекта, вертикаль 9:16, фиксированная камера,
   с одной яркой деталью-интригой (дерево в окне, паровоз в земле, плющ по стенам). Без людей в кадре.
2. Каждую следующую стадию делай **редактированием предыдущего кадра** (не новой генерацией!),
   меняя только то, что физически поменялось бы на стройке: **стадия 1 — ТОЛЬКО уборка территории**
   (убрали растения/мусор, ничего конструктивного ещё не изменилось), дальше леса и демонтаж →
   новый каркас/крыша → окна и отделка → финал в тёплом свете. Не смешивай несколько стадий в один
   шаг — иначе кадр останется «грязным» и прогресс не будет читаться. Так сохраняются ракурс, свет,
   геометрия. Лучшие инструменты: FLUX Kontext (`mcp__Bfl_ai__generate_image` с входным изображением),
   Grok Imagine (image edit, не text-to-image), Google Flow (Edit/Remix с входным кадром).
3. **В статичных кадрах-стадиях НЕТ людей, техники и инструментов.** Кадр — это редактирование
   предыдущего кадра, так что любой рабочий на нём скопируется пиксель в пиксель на все следующие
   стадии и «замрёт» в одной позе — это сразу видно и выглядит как брак. Люди и техника — только
   в видео-переходах (шаг 4), и в каждом переходе — разное действие.
4. Каждую стадию **проверяй глазами** — не «поплыла» ли геометрия (окна, число этажей, деревья).
   Если сама не видишь результат (сеть/доступ) — не выдавай его как готовый, попроси посмотреть пользователя.
Шаблоны: `references/prompts.md` → «Цепочка стадий».

**Немного генераций, но качественных.** 5–6 кадров стадий = 4–5 клипов-переходов — этого достаточно
на 60–90 секунд, если делать переходы подлиннее, а не дробить на десятки мелких вставок.

### 4. Анимация между стадиями
Для каждой пары (кадр N → кадр N+1) — один клип, где та же статичная камера показывает, как рабочие
меняют объект от одного состояния к другому:
- **Google Flow → Frames to Video** (Veo 3.x) — лучший вариант: загружаешь кадр N как первый кадр
  и кадр N+1 как последний, Flow сам рисует движение и звук стройки между ними. Формат 9:16.
- **Grok Imagine → Image to Video** — загружаешь кадр N как стартовую картинку, в промпте описываешь
  само действие («workers strip the ivy and replace the roof beams, static camera, time-lapse»).
  Grok не умеет фиксировать последний кадр — просто следи глазами, что результат похож на кадр N+1,
  и при необходимости перегенерируй.
- `mcp__Bfl_ai__generate_video` — умеет переход между двумя ключевыми кадрами прямо из этой сессии.
- **Если клип получается коротким, а нужен длиннее:** возьми последний кадр готового клипа
  (скриншот/стоп-кадр) и продолжи с него следующий клип тем же промптом — это единственный случай,
  когда нужна ручная передача кадра между генерациями.
Сохраняй клипы как `clips/01.mp4`, `clips/02.mp4`… по порядку.

**Альтернатива (не по умолчанию):** для канала, который хочет больше «мяса» — крупных планов рук,
инструментов, ASMR-звука — можно вместо цепочки стадий сделать шот-лист из отдельных коротких
экшн-сценок. Это стилистический выбор, не замена: см. `references/prompts.md` → «Шот-лист действий
(опционально)». По умолчанию используй статичную цепочку стадий.

### 5. Сборка
```bash
bash .claude/skills/restoration-timelapse-shorts/scripts/assemble.sh episodes/<slug> \
  --speed 1.25 --music episodes/<slug>/audio/music.mp3 --hook "Этой башне 112 лет"
```
Результат: `out/final.mp4` (1080×1920, 30 fps, H.264, AAC, −14 LUFS), `out/first.jpg` и `out/last.jpg`
для проверки петли. Цель 20–35 с. Если в пути нужен свой ffmpeg — `FFMPEG=/путь/к/ffmpeg`.
Для тонкого монтажа (субтитры, переходы) можно подключить скилл `video-edit`.

### 6. Упаковка
Из `plan.md` бери блок метаданных и доработай:
- **Заголовок** ≤ 60 символов, с реальным местом/годом/цифрой. Пометка `[AI concept]` / `[ИИ-концепт]`
  в конце — снижает негатив в комментариях и не мешает охватам (у Bau Rausch 127M с такой пометкой).
- **Описание**: 2 строки истории объекта + вопрос в комментарии («Что восстановить следующим?»).
- 3–5 хэштегов: `#restoration #timelapse #beforeandafter` + тематический (`#abandoned`, `#train`).
- В YouTube Studio включи **«Altered or synthetic content»** — это требование YouTube для реалистичного AI.
- Опционально заголовок/описание на EN, DE, ES, PT — YouTube покажет их зрителям этих стран.

### 7. Проверка перед выдачей
- Первые 1,5 с — самая драматичная точка разрухи, без логотипов и заставок.
- Нет «плавающей» геометрии между стадиями, нет лишних пальцев у рабочих, нет водяных знаков.
- Последний кадр — финальный «вау», по возможности визуально стыкуется с первым (петля → досмотры).
- Громкость нормализована, музыка не глушит звуки стройки.

## Автоматизация и пакетный режим
- 5–10 эпизодов за раз: запускай `new_episode.py` для каждой темы, затем генерируй кадры пачкой.
- Повторяющийся контент-план — через Routines/`/loop` (например, «каждый понедельник: 7 идей + промпты»).
- Не публикуй одинаковые шаблонные ролики сериями без вариаций — YouTube отключает монетизацию
  за «inauthentic / mass-produced content». Меняй объект, историю, звук, структуру.
